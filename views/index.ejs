<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/static/pokeCard.css" />
    <title>Index of Pokemon</title>
  </head>
  <body>
    <div class="border">
      <div class="metalBG"></div>
    </div>

    <div class="container">
      <% for(let i = 0; i < data.length; i+=1){ %>
      <h2><%=data[i].name %></h2>
      <img src="<%=data[i].img %> " alt="Image Not Found" />
      <ul>
        <% const type = data[i].type %> <% for(let j = 0; j < type.length;
        j+=1){ %>
        <li><%=type[j] %></li>
        <% } %>
      </ul>
      <h3>HP: <%=data[i].stats.hp %></h3>
      <h3>Attack: <%=data[i].stats.attack %></h3>
      <h3>Defense: <%=data[i].stats.defense %></h3>
      <% } %>
    </div>
  </body>
</html>
